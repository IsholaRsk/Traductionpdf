# Dépendances OPTIONNELLES — TradFilez fonctionne sans elles.
# Chacune n'ouvre qu'un format de plus ; le serveur les ignore si elles manquent.
python-docx>=1.1     # .docx
openpyxl>=3.1        # .xlsx
PyMuPDF>=1.24      # .pdf (mise en page conservée)
pypdf>=5.0           # .pdf (repli : texte seul, si PyMuPDF absent)
beautifulsoup4>=4.12 # .html (nœud par nœud, doctype et scripts préservés)
