# Socle : Flask sert seulement d'adaptateur HTTP pour l'hébergeur sans état.
flask>=3.0

# Les mêmes bibliothèques optionnelles qu'en local : sans elles, le site tourne
# mais .docx, .xlsx, .pdf et .html retombent sur des parseurs plus simples.
python-docx>=1.1
openpyxl>=3.1
pypdf>=4.2
beautifulsoup4>=4.12
lxml>=5.2
